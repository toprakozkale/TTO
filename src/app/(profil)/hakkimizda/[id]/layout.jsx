// (profil)/hakkimizda/[id]/layout.jsx
// ProfileShell yönetimi page.jsx içinde yapıldığı için
// bu layout sadece sade bir wrapper sağlar.
export default function ProfileLayout({ children }) {
    return <>{children}</>;
}
